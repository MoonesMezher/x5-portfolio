#1 MOONES MEZHER:
- Setup project (react vite, react icons, react router dom)
- Install all required (images, fonts)
- Create the repo
- Handle project structure src/(api, assets, components, context, hooks, pages, types)
- Create the main classes in css like (main-container, page)
- Handle pages layout (Header, Footer)
- Seperate the project to small tasks
- Design all components which started in className has mm-
- Design shared components like (MainButton, MainTitle, MainLink, MainInput, Alert)
- Create a helper (stringLimit)
- Make all components fit with eact other
- Create Admin context
- Handle login and logout functions (with api)
- Handle edit project, create message (api)
- Create (dashboard, dashboardSection)

#2 NEVEN GHASOUN:
- Design MyProject section 
- Design CardProject components 
- Use axios.post to push data in the projectRequest Component

#3 DIANA HAMMAMEH:
- Create Hero section
- Handle create messages (api)

#4 RAMI ALZEND:

#5 FIRAS ALSEMGY:

#6 ESMAEEL ABD: